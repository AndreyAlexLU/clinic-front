// @flow
import React, { Component } from 'react';

type Props = {||};

export default class Footer extends Component<Props, *> {
    render() {
        return (
            <div>
            
            </div>
        );
    }
}
